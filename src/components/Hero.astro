---
// const personalImageAlt = "Rodrigo Acuña"
import Badge from "@/components/Badge.astro"
import LinkedInIcon from "@/components/icons/LinkedIn.astro"
import MailIcon from "@/components/icons/Mail.astro"
import GitHubIcon from "./icons/GitHub.astro"
import SocialPill from "@/components/SocialPill.astro"
import Curriculum from "./icons/Curriculum.astro"

import { getLangFromUrl, useTranslations } from "@/i18n/utils"

type tLang = (str: any) => any;

const lang = getLangFromUrl(Astro.url)
const t:tLang = useTranslations(lang);
---

<div class="max-w-xl lg:bg-gray-100 lg:dark:bg-[#1A1A1A] p-4 lg:rounded-[25px] lg:shadow-lg">
  <div class="flex gap-4 mb-4">
    <img
      class="rounded-full shadow-lg size-16"
      src="https://github.com/joanmat117.png"
      alt={t('personalImageAlt')}
    />
    <a
      href="https://www.linkedin.com/in/joan-matias-dominguez-3652a1365/"
      target="_blank"
      rel="noopener"
      class="flex items-center transition md:justify-center"
    >
      <Badge>{t('hero.badge')}</Badge>
    </a>
  </div>
  <h1
    class="text-4xl font-bold tracking-tight text-gray-800 sm:text-5xl dark:text-white text-balance"
  >
    {t('hero.title')}
  </h1>
  <p
    set:html={t('hero.bio')}
    class="
      mt-6
      text-xl
      text-gray-800
      dark:[&>strong]:[#42b883]
      [&>strong]:text-[#42b883]
      [&>strong]:font-semibold
      dark:text-gray-300
      text-pretty"
  >
  </p>
  <nav class="flex flex-wrap gap-4 mt-8">
    <SocialPill href="mailto:joanmat@proton.me">
      <MailIcon class="size-4" />
      {t('hero.mail')}
    </SocialPill>
    <SocialPill href="https://www.linkedin.com/in/joan-matias-dominguez-3652a1365/">
      <LinkedInIcon class="size-4" />
      LinkedIn
    </SocialPill>
    <SocialPill href="https://github.com/joanmat117">
      <GitHubIcon class="size-4" />
      Github
    </SocialPill>
    <SocialPill download={`CV_Joan_Matias_${lang.toUpperCase()}`} href={`/joan_cv_${lang || 'es'}.pdf`}>
      <Curriculum class="size-4" />
      CV
    </SocialPill>
  </nav>

  <article>
    
  </article>

</div>
